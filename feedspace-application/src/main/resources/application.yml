spring:
  application:
    name: feedspace
  jpa:
    open-in-view: false

---
# DEV профиль (H2) - с использованием schema.sql
spring:
  config:
    activate:
      on-profile: dev
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:feedspace;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;INIT=CREATE SCHEMA IF NOT EXISTS public
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: validate  # ← МЕНЯЕМ: только проверка, не создает таблицы
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_schema: public
  sql:
    init:  # ← ДОБАВЛЯЕМ: инициализация из SQL файлов
      mode: always
      schema-locations: classpath:schema.sql
      # data-locations: classpath:data-dev.sql  # если будешь тестовые данные
      continue-on-error: false  # продолжать если есть ошибки


server:
  port: 8080
  error:
    include-stacktrace: always

---
# PROD профиль (PostgreSQL) - используем schema.sql
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:postgresql://localhost:5432/feedspace
    username: postgres
    password: ${DB_PASSWORD:changeme}
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate  # Только проверка структуры
    show-sql: false
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      # data-locations: classpath:data-prod.sql (опционально)